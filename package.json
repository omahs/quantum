{
  "name": "bridge.defichain.com",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "write": "turbo run write",
    "clean": "turbo run clean",
    "build": "turbo run build",
    "build:docker": "turbo run build:docker",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@defichain/jellyfish-address": "^3.5.2"
  },
  "devDependencies": {
    "@birthdayresearch/sticky-turbo": "^0.3.1",
    "husky": "^8.0.2",
    "lint-staged": "^13.1.0"
  },
  "engines": {
    "node": "^18.0.0 || ^16.16.0",
    "pnpm": ">=7.11.0"
  }
}
